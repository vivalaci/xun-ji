/* pages/body/edit.wxss */
.form { padding: 8rpx 32rpx; }
.field { padding: 30rpx 0; border-bottom: 1rpx solid var(--border); }
.field.no-border { border-bottom: none; }
.label { color: var(--muted); }
.picker-val { color: var(--text); }
.input { text-align: right; min-width: 240rpx; }
.waist-tip { font-size: 24rpx; line-height: 1.6; padding: 16rpx 8rpx 0; }
.save-btn { margin-top: 32rpx; }
